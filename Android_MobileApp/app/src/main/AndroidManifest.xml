<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chubby">

        <!-- Launcher = Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Các màn còn lại KHÔNG có intent-filter -->
        <activity android:name=".RegisterActivity" android:exported="false"/>
        <activity android:name=".MainActivity" android:exported="false"/>

        <!-- Giữ duy nhất 1 khai báo cho DishDetailActivity -->
        <activity
            android:name=".DishDetailActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <activity android:name=".BmiActivity" android:exported="false"/>
        <activity android:name=".ui.feed.BlogActivity" android:exported="false"/>
        <activity
            android:name=".ui.feed.CreatePostActivity"
            android:exported="false"/>
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".CreateDishActivity"
            android:exported="false"
            android:label="Thêm món ăn" />
        <activity android:name=".ui.comment.CommentActivity"
            android:exported="false"/>
        <activity android:name=".ui.admin.AdminDashboardActivity"
            android:exported="true"/>

    </application>
</manifest>
